[package]
name = "gemma4-prompt-builder-core"
version = "0.1.0"
description = "Shared formatting engine and data structures for Gemma 4 prompt generation."
authors = ["GregTheRick"]
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
